let Joi,User,bcrypt,JwtService;_031‍.x([["default",()=>_031‍.o]]);_031‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_031‍.w("../../models",[["User",["User"],function(v){User=v}]]);_031‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_031‍.w("../../services/JwtServices",[["default",["JwtService"],function(v){JwtService=v}]]);




const registerController = {
    async register(req,res,next){


        const registerSchema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}')).required(),
        });

        const { error } = registerSchema.validate(req.body);

        if(error){
            //return next( error );
            throw error;
        };

        //hash password

        const hashedPassword = await bcrypt.hash(req.body.password,10);

        //prepare the model

        const user = new User({
            email: req.body.email,
            password: hashedPassword,
        });

        let access_token;


        try{
            const result = await user.save();

            //token
            access_token= JwtService.sign({ _id: result._id})


        }catch(err){
            return error;
        }
        res.json({access_token:access_token})


    }
}


_031‍.d(registerController);

